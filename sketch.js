function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(255, 0, 0); // Der Bildschirm MUSS jetzt knallrot werden
  fill(255);
  textSize(32);
  text("Verbindung steht!", 50, 50);
}